package jr.service.routeService;

import jr.domain.route.Route;
import org.springframework.stereotype.Service;

@Service
public class RouteService {
    public void check(Route route){
//        if (!(route.getDeparture().getStation().getStationName().getName().equals("東京"))) {
////            if(!(route.getDestination().getStation().getStationName().equals("姫路") ||
////                    route.getDestination().getStation().getStationName().equals("新大阪")))
//            throw new RuntimeException("すみません。東京-姫路と東京-新大阪区間しか対応してません。");
//        }
    }
}
